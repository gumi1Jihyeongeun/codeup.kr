#include<stdio.h>
int main(){
    int a, b, c, d;
    double result;

    scanf("%d %d %d %d", &a, &b, &c, &d);
    result = a * b * c * d / 8;
    result = result / 1024;
    result = result / 1024;
    printf("%.1f MB", result);

    return 0;
}
